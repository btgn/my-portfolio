import { Card, CardContent } from '@mui/material';

const landingPage = () => {
  return (
    <Card>
      <CardContent> Landing Pages </CardContent>
    </Card>
  );
};

export default landingPage;
